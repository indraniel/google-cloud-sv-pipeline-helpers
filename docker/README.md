# production mode

    docker build -t sv-lsort-helper:v1 .
    docker run -i -t --rm sv-lsort-helper:v1 /bin/bash

# Development Mode

    docker build -t sv-lsort-helper:v1 .
    docker run -i -t --rm sv-lsort-helper:v1 /bin/bash

# Testing

    docker build -t sv-lsort-helper:v1 .
    docker run -i -t --rm sv-lsort-helper:v1 /bin/bash

